{
  "buildCommand": "pnpx convex deploy --typecheck=disable --cmd 'pnpm run build'",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
